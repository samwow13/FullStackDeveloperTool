<Window x:Class="FullStackLauncher.DatabaseToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FullStackLauncher"
        Title="Database · Compare &amp; migrate" Width="1280" Height="940" MinWidth="980" MinHeight="740"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI" FontSize="13" UseLayoutRounding="True"
        WindowStartupLocation="CenterOwner" Icon="/Assets/launcher.ico" Closing="Window_Closing">
    <Window.Resources>
        <!-- Derived windows do not pick up the implicit Window style. Keep this
             workspace's content surfaces explicit, including the selected tab. -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template"><Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <TabPanel IsItemsHost="True" KeyboardNavigation.TabIndex="1" Margin="0,0,0,8"/>
                        <Border Grid.Row="1" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value></Setter>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem">
                <Border x:Name="Frame" Background="#223549" BorderBrush="#3A516C" BorderThickness="1" CornerRadius="6" Padding="16,10" Margin="0,0,8,0">
                    <ContentPresenter x:Name="HeaderContent" ContentSource="Header" RecognizesAccessKey="True" TextElement.Foreground="{TemplateBinding Foreground}"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Frame" Property="Background" Value="#30465C"/></Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="Frame" Property="Background" Value="#203C3B"/>
                        <Setter TargetName="Frame" Property="BorderBrush" Value="#69E2C0"/>
                        <Setter TargetName="HeaderContent" Property="TextElement.Foreground" Value="{StaticResource AccentBrush}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource AccentBrush}"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="IsReadOnly" Value="True"/><Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/><Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/><Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="Background" Value="#111923"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="RowBackground" Value="#111923"/><Setter Property="AlternatingRowBackground" Value="#1D2938"/>
            <Setter Property="AlternationCount" Value="2"/><Setter Property="MinRowHeight" Value="34"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/><Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="ClipboardCopyMode" Value="IncludeHeader"/>
        </Style>
        <Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#293B50"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/><Setter Property="Padding" Value="9,8"/></Style>
        <Style TargetType="DataGridCell"><Setter Property="Padding" Value="7,5"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="DataGridCell">
                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                    <ContentPresenter VerticalAlignment="Center"/>
                </Border>
            </ControlTemplate></Setter.Value></Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#285D55"/></Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter Property="BorderThickness" Value="1"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CodeText" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True"/><Setter Property="FontFamily" Value="Cascadia Code, Consolas"/><Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/><Setter Property="Background" Value="#111923"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/><Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <Style x:Key="ReadoutText" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True"/><Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/><Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="FontSize" Value="14"/><Setter Property="Block.LineHeight" Value="23"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </Window.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="Compare databases &amp; apply migrations" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="Compare the source schema with the target, then review repository migrations for the target database." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/>
        </StackPanel>
        <Grid x:Name="Endpoints" Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <local:DatabaseEndpointPicker x:Name="SourceEndpoint"/>
            <local:DatabaseEndpointPicker x:Name="TargetEndpoint" Grid.Column="2"/>
        </Grid>
        <WrapPanel Grid.Row="2" Margin="0,12,0,4">
            <Button x:Name="CompareButton" Content="Compare schemas" Click="Compare_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8"/>
            <Button x:Name="SwapButton" Content="Swap source / target" Click="Swap_Click" Margin="0,0,8,8"/>
            <Button x:Name="ReloadButton" Content="Reload connections" Click="Reload_Click" Margin="0,0,8,8"/>
            <Button x:Name="ExportComparisonButton" Content="Export comparison…" Click="ExportComparison_Click" IsEnabled="False" Margin="0,0,8,8"/>
            <Button x:Name="CancelButton" Content="Cancel operation" Click="Cancel_Click" IsEnabled="False" Margin="0,0,0,8"/>
        </WrapPanel>
        <TabControl x:Name="WorkspaceTabs" Grid.Row="3">
            <TabItem Header="Schema _differences">
                <Grid TextElement.FontWeight="Normal">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="7"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock x:Name="ComparisonSummary" Text="Choose any two PostgreSQL databases and click Compare schemas. Comparison does not change either database." TextWrapping="Wrap" Margin="0,0,0,8"/>
                    <DockPanel Grid.Row="1" Margin="0,0,0,10">
                        <ComboBox x:Name="ChangeFilter" DockPanel.Dock="Right" Width="190" SelectionChanged="Filter_Changed" Margin="8,0,0,0" SelectedIndex="0" AutomationProperties.Name="Filter change type">
                            <ComboBoxItem Content="All differences"/><ComboBoxItem Content="Only in source"/><ComboBoxItem Content="Only in target"/><ComboBoxItem Content="Definition changed"/>
                        </ComboBox>
                        <Label Content="_Search" Target="{Binding ElementName=Search}" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Padding="0,0,10,0"/>
                        <TextBox x:Name="Search" TextChanged="Search_Changed" AutomationProperties.Name="Search schema differences" ToolTip="Filter by object type, schema or name"/>
                    </DockPanel>
                    <DataGrid x:Name="DifferenceGrid" Grid.Row="2" SelectionChanged="Difference_Changed" AutomationProperties.Name="Schema differences">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Difference" Binding="{Binding ChangeLabel}" Width="140"/>
                            <DataGridTextColumn Header="Object type" Binding="{Binding Kind}" Width="150"/>
                            <DataGridTextColumn Header="Schema" Binding="{Binding Schema}" Width="130"/>
                            <DataGridTextColumn Header="Object" Binding="{Binding Name}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch" Height="4" Background="{StaticResource BorderBrush}" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext" AutomationProperties.Name="Resize schema definition details"/>
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="SOURCE DEFINITION" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,6"/><TextBox Grid.Row="1" x:Name="SourceDefinition" Style="{StaticResource CodeText}" AutomationProperties.Name="Source object definition"/></Grid>
                        <Grid Grid.Column="2"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="TARGET DEFINITION" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,6"/><TextBox Grid.Row="1" x:Name="TargetDefinition" Style="{StaticResource CodeText}" AutomationProperties.Name="Target object definition"/></Grid>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem x:Name="MigrationTab" Header="_Migrations &amp; SQL">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid TextElement.FontWeight="Normal">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="110"/><RowDefinition Height="Auto"/><RowDefinition Height="240"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Grid x:Name="MigrationInputs">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="200"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Label Content="EF migrations _project" Target="{Binding ElementName=ProjectPicker}" Foreground="{StaticResource TextBrush}" Padding="0,0,0,6"/>
                        <Label Grid.Column="2" Content="_History view" Target="{Binding ElementName=HistorySide}" Foreground="{StaticResource TextBrush}" Padding="0,0,0,6"/>
                        <ComboBox Grid.Row="1" x:Name="ProjectPicker" IsEditable="True" IsTextSearchEnabled="False" AutomationProperties.Name="EF migrations project path" ToolTip="Select or enter the API .csproj containing migrations"/>
                        <Button Grid.Row="1" Grid.Column="1" Content="Browse…" Click="BrowseProject_Click" Margin="8,0"/>
                        <ComboBox Grid.Row="1" Grid.Column="2" x:Name="HistorySide" SelectedIndex="0" SelectionChanged="HistorySide_Changed" AutomationProperties.Name="Migration history view"><ComboBoxItem Content="Repository / target"/><ComboBoxItem Content="Source / target history"/></ComboBox>
                        <Button x:Name="PrepareButton" Grid.Row="1" Grid.Column="3" Content="Prepare migration plan" Click="Prepare_Click" Margin="8,0,0,0"/>
                    </Grid>
                    <TextBlock Grid.Row="1" Text="Preparation builds the selected project and generates forward migration SQL. Only Apply reviewed migrations changes the selected target." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,10,0,6"/>
                    <TextBlock Grid.Row="2" x:Name="MigrationSummary" Text="Compare databases to see EF history, or prepare a plan to check the target against repository migrations." TextWrapping="Wrap" Margin="0,4,0,7"/>
                    <DataGrid Grid.Row="3" x:Name="MigrationGrid" AutomationProperties.Name="EF migration history">
                        <DataGrid.Columns><DataGridTextColumn Header="Migration" Binding="{Binding Id}" Width="*"/><DataGridTextColumn Header="Source / repository" Binding="{Binding SourceStatus}" Width="180"/><DataGridTextColumn Header="Target" Binding="{Binding TargetStatus}" Width="180"/></DataGrid.Columns>
                    </DataGrid>
                    <DockPanel Grid.Row="4" Margin="0,8,0,6"><Button x:Name="ExportSqlButton" DockPanel.Dock="Right" Content="Export SQL…" Click="ExportSql_Click" IsEnabled="False" Padding="10,5"/><TextBlock x:Name="SqlHeading" Text="REVIEW SQL" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/></DockPanel>
                    <TextBox Grid.Row="5" x:Name="SqlPreview" Style="{StaticResource CodeText}" AutomationProperties.Name="Reviewed migration SQL"/>
                    <Border Grid.Row="6" Background="#28222B" BorderBrush="#66404C" BorderThickness="1" CornerRadius="7" Padding="10" Margin="0,9,0,0">
                        <StackPanel>
                            <TextBlock x:Name="ConfirmationLabel" Text="Prepare a plan to review the exact target and enable migration application." TextWrapping="Wrap"/>
                            <CheckBox x:Name="BackupCheck" Foreground="{StaticResource TextBrush}" HorizontalContentAlignment="Stretch" Checked="Confirmation_Changed" Unchecked="Confirmation_Changed" Margin="0,10,0,10"
                                      AutomationProperties.Name="I reviewed this SQL, have a recovery backup, and the target is ready for these changes.">
                                <TextBlock Text="I reviewed this SQL, have a recovery backup, and the target is ready for these changes." TextWrapping="Wrap"/>
                            </CheckBox>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBox x:Name="TargetConfirmation" TextChanged="ConfirmationText_Changed" AutomationProperties.Name="Type the exact target to confirm migrations"/>
                                <Button x:Name="ApplyButton" Grid.Column="1" Content="Apply reviewed migrations" Click="Apply_Click" Style="{StaticResource DangerButton}" IsEnabled="False" Margin="8,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="_Coverage &amp; results">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel MaxWidth="940" HorizontalAlignment="Left" TextElement.FontWeight="Normal" Margin="0,0,10,0">
                        <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                            <StackPanel>
                                <TextBlock Text="What this comparison means" Foreground="{StaticResource TextBrush}" FontSize="19" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <TextBox x:Name="CoverageText" Style="{StaticResource ReadoutText}" AutomationProperties.Name="Comparison coverage and limitations"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,14,0,0">
                            <StackPanel>
                                <TextBlock Text="Operation results" Foreground="{StaticResource TextBrush}" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <TextBlock Text="No operation results yet. Results will appear here as you work." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap">
                                    <TextBlock.Style><Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers><DataTrigger Binding="{Binding Text, ElementName=OperationResults}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                    </Style></TextBlock.Style>
                                </TextBlock>
                                <TextBox x:Name="OperationResults" Style="{StaticResource ReadoutText}" AutomationProperties.Name="Database operation results"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        <Border Grid.Row="4" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12,9" Margin="0,12,0,0">
            <TextBlock x:Name="StatusText" Text="Ready." TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
        </Border>
    </Grid>
</Window>
